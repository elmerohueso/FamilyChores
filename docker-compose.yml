version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - db_data:/data
    environment:
      - FLASK_ENV=development
    restart: unless-stopped

volumes:
  db_data:
